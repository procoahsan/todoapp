version: "3.8"
services:
  mongodb:
    image: 'mongo'
    volumes:
      - data:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=ahsannadeem
      - MONGO_INITDB_ROOT_PASSWORD=Homosapien
    ports:
      - '27017:27017'

  server:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - mongodb

volumes:
  data: